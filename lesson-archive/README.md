# Lesson Archive

This folder stores Java files from completed lessons.

Files here are kept for review, but they are outside `src` so they do not clutter
the active lesson workspace.

Use `src` for the current lesson only.
