# Lesson 001: First Java Run

Archived files:

- `HelloJava.java`: the first Java file that printed `Hello Java`
- `Main.java`: IntelliJ starter file that was not needed for the next lesson

These files were moved out of `src` so the next lesson can start with a clean
workspace.
